# Infrastructure

- `terraform/` — IaC для Timeweb Cloud (VPS + firewall + DNS + cloud-init).
- `deploy/` — runtime-файлы и скрипты для ручного прод-деплоя backend + web на сервере.

Старт: `infra/terraform/README.md`.
