package com.apofeoz.backend.domain

enum class Role { USER, FOREMAN, ADMIN }

enum class UserStatus { ACTIVE, INACTIVE }

enum class WorkerStatus { ACTIVE, INACTIVE }

enum class SessionStatus { ACTIVE, CLOSED }
